FROM node:22-alpine3.22

RUN apk add --no-cache ca-certificates postgresql17-client

WORKDIR /app
COPY src/ ./
RUN chmod 0555 scripts/bootstrap-azure-database-gate.sh \
  scripts/run-azure-database-gate.sh

CMD ["sh", "scripts/bootstrap-azure-database-gate.sh"]
